{
  "version": "0.0.0",
  "private": true,
  "name": "WebRtcApp",
  "description": "A client webRtc",
  "devDependencies": {
	"http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install",

    
    "start": "http-server -a 0.0.0.0 -p 3000"
    }
}
